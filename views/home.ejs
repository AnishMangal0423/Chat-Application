<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Group Names</title>
</head>
<body>
    <h3 style="margin-left: 35px;">Groups -</h3>

    <ul>
        <% Groups.forEach(group => { %>
            <li style="list-style: none; margin-top: 10px;"><a href="/chat/<%= group._id %>/your-username-here" style="text-decoration: none;"><%= group.name %></a></li>
        <% }) %>
    </ul>
</body>
</html>